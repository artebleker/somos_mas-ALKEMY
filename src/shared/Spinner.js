import React from 'react';
import { Oval } from 'react-loader-spinner';

const Spinner = () => {
	return (
		<div>
			<Oval color='#214763' height={80} width={80} />
		</div>
	);
};

export default Spinner;
